%h1 Control Panel

-if @user.venues.any?
  %ul
    -@user.venues.each do |v|
      %li
        =succeed ' ' do
          =v.name        
          =link_to 'Feedback', new_venue_feedback_url(v)
          =link_to 'Resources', venue_path(v)

= link_to 'Add Venue', new_venue_path